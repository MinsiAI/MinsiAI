spring:
  config:
    activate:
      on-profile: dev

minsi:
  app:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001}
    cookie-secure: ${COOKIE_SECURE:false}
